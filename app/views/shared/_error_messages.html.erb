<% if @player.errors.any? %>
	<div class = "error_messages">
		<h2> The form contains <%= pluralize(@player.errors.count, "error") %>.
		<ul>
			<% @player.errors.full_messages.each do |msg| %>
				<li> <%= msg %> </li>
			<% end %>
		</ul>
	</div>
<% end %>