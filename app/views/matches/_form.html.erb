<%= form_for [player, match] do |f| %>
	<%= f.label :course %>
	<%= f.collection_select(:course_id, @courses, :id, :name) %>

	<%= f.label :tee %>
	<%= f.collection_select(:tee_id, @tees, :id, :colour) %> 

	<%= f.label :end_date, "Expiration Date: Two Weeks Max" %>
	<%= f.month_field :end_date, max: 14.days.from_now %>	

	<%= f.hidden_field :player_id, :value => player.id %>

	<%= f.submit %> 
<% end %>

