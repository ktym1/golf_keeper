<%= form_for [player, round] do |f| %>
	<%= f.label :course %>
	<%= f.collection_select(:course_id, @courses, :id, :name) %>


	<%= f.label :round_length %>


	<%= f.label :start_hole %>
	<%= f.number_field :start_hole %>
	
	<%= f.submit %> 
<% end %>



